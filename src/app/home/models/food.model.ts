export class Food {
    title: string;
    des: string;
    img: string;

    constructor( title: string, des: string, img: string) {
        this.title = title;
        this.des = des;
        this.img = img;
    }
}